# Python dependencies for fit_hedonic.py. Everything else it uses — json, math,
# sqlite3, collections, time — is stdlib.
#
# Pinned to the versions that produced the coefficients currently deployed
# (backend/src/pricing-v2-coeffs.json, HEDONIC_MODEL_VERSION 2.0.0), built under
# Python 3.13.2. Ridge results are numerically sensitive to the solver, so a refit
# meant to be comparable to the live model should start from these; loosen them
# deliberately, not by accident.
#
# The venv lives at "sales database/.venv" and is gitignored there. It already
# exists on the working machine — check before creating one, and never put it in
# a temp dir: a scratch venv outside the project is wiped between sessions and
# then looks like the packages have gone missing.
#
#   python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
#   .venv/bin/python fit_hedonic.py
#
numpy==2.5.2
scikit-learn==1.9.0
